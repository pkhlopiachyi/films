.film-carousel {
  display: flex;
  max-width: 880px;
  flex-wrap: wrap;
  margin: 0 auto;

  &__item {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    width: 200px;
    height: 380px;
    cursor: pointer;
    position: relative;
    margin-right: 20px;
    transition: .4s;


    &__delete {
      background-color: #000000;
      position: absolute;
      top: 25px;
      right: 0;
      width: 24px;
      height: 24px;
      display: none;
      justify-content: center;
      align-items: center;
      border-radius: 50%;

      svg {
        path {
          fill: #ffffff;
        }
      }
    }

    &__image {
      img {
        width: 200px;
        height: 270px;
        object-fit: contain;
      }
    }

    &__title {
      height: 40px;
      text-align: center;
      font-size: 20px;
      color: #ffffff;
    }

    &:hover {
      transform: scale(1.1);

      .film-carousel__item__delete {
        display: flex;
        transform: scale(0.7);
      }
    }
  }

  &__empty {
    width: 100%;
    height: 200px;
    display: flex;
    align-items: center;
    padding: 0 10px;
  }
}

.no-data {
  overflow: hidden
}

@media (max-width: 576px) {
  .film-carousel {
    flex-direction: column;

    &__item {
      width: 100%;
    }
  }
}